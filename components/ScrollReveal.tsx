import React from 'react';

// This component is deprecated. 
// All scroll animation logic is now centralized in components/ScrollAnimation.tsx
// ensuring a single, performant IntersectionObserver instance.
const ScrollReveal: React.FC = () => {
  return null;
};

export default ScrollReveal;